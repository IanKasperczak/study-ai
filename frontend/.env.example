NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Debe coincidir con MAX_UPLOAD_MB del backend (backend/.env.example) -- solo
# evita una subida lenta que el backend igual va a rechazar.
NEXT_PUBLIC_MAX_UPLOAD_MB=20

